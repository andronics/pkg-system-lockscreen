name="lockscreen"
description="Lockscreen Manager - Screen locking and idle control"
version="0.1.0"
method="stow"
collection="system"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=()
ignore=(
    "README.md"
)
